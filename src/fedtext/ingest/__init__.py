"""Fed text ingestion package.

Scope of ``fedtext.ingest``:
  - speeches/documents discovery + fetch + parse
  - catalog DB migrations
  - ingestion-time data quality validators

Time-series target logic intentionally lives under ``fedtext.targets``.
"""
